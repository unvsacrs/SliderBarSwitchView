import XCTest
@testable import SliderBarSwitchView

final class SliderBarSwitchViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SliderBarSwitchView().text, "Hello, World!")
    }
}
